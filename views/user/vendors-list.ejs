<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Browse Vendors</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: #f4f4f4;
            padding: 20px;
        }
        header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 15px 20px;
            border-radius: 4px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 40px;
        }
        .header-right { display: flex; gap: 15px; }
        .header-right a {
            color: white; text-decoration: none; padding: 8px 15px;
            background: rgba(255,255,255,0.2); border-radius: 4px;
        }
        .container { max-width: 1200px; margin: 0 auto; }
        .filter-section {
            background: white;
            padding: 20px;
            border-radius: 8px;
            margin-bottom: 30px;
            display: flex;
            gap: 15px;
            align-items: center;
        }
        .filter-section label {
            font-weight: bold;
            color: #34495e;
        }
        .filter-section select {
            padding: 8px 12px;
            border: 1px solid #ddd;
            border-radius: 4px;
            cursor: pointer;
        }
        .filter-section button {
            padding: 8px 15px;
            background: #667eea;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        .vendors-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
            gap: 20px;
        }
        .vendor-card {
            background: white;
            border-radius: 8px;
            overflow: hidden;
            box-shadow: 0 2px 8px rgba(0,0,0,0.1);
            transition: transform 0.3s;
        }
        .vendor-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 5px 15px rgba(0,0,0,0.2);
        }
        .vendor-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            text-align: center;
        }
        .vendor-header h3 {
            font-size: 18px;
            margin-bottom: 5px;
        }
        .vendor-header p {
            font-size: 12px;
            background: rgba(255,255,255,0.2);
            padding: 5px 10px;
            border-radius: 3px;
            display: inline-block;
        }
        .vendor-body {
            padding: 15px;
        }
        .vendor-body p {
            color: #7f8c8d;
            font-size: 14px;
            margin-bottom: 10px;
        }
        .products-count {
            background: #f0f0f0;
            padding: 10px;
            border-radius: 4px;
            text-align: center;
            color: #34495e;
            font-weight: bold;
        }
        .vendor-footer {
            padding: 15px;
            border-top: 1px solid #eee;
        }
        .browse-btn {
            width: 100%;
            padding: 10px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            font-weight: bold;
        }
        .browse-btn:hover {
            opacity: 0.9;
        }
        .back-btn {
            display: inline-block;
            padding: 8px 15px;
            background: #666;
            color: white;
            text-decoration: none;
            border-radius: 4px;
            margin-bottom: 20px;
        }
        .empty-state {
            text-align: center;
            padding: 60px 20px;
            color: #7f8c8d;
        }
    </style>
</head>
<body>
    <header>
        <h2>Browse Vendors</h2>
        <div class="header-right">
            <a href="/user/dashboard">Home</a>
            <a href="/user/cart">Cart (<span id="cartCount"><%= cartItemCount %></span>)</a>
            <a href="/logout">LogOut</a>
        </div>
    </header>

    <div class="container">
        <a href="/user/dashboard" class="back-btn">← Back</a>

        <div class="filter-section">
            <label for="categoryFilter">Filter by Category:</label>
            <select id="categoryFilter">
                <option value="">All Categories</option>
                <option value="Catering">Catering</option>
                <option value="Florist">Florist</option>
                <option value="Decoration">Decoration</option>
                <option value="Lighting">Lighting</option>
            </select>
            <button onclick="filterVendors()">Filter</button>
        </div>

        <% if (vendors.length === 0) { %>
            <div class="empty-state">
                <h2>No Vendors Available</h2>
                <p>Please check back later for vendor services.</p>
            </div>
        <% } else { %>
            <div class="vendors-grid">
                <% vendors.forEach(vendor => { %>
                    <div class="vendor-card">
                        <div class="vendor-header">
                            <h3><%= vendor.vendor_name %></h3>
                            <p><%= vendor.category %></p>
                        </div>
                        <div class="vendor-body">
                            <p><strong>Email:</strong> <%= vendor.email %></p>
                            <p><strong>Phone:</strong> <%= vendor.phone %></p>
                            <div class="products-count"><%= vendor.product_count %> Products</div>
                        </div>
                        <div class="vendor-footer">
                            <button class="browse-btn" onclick="browseVendor('<%= vendor.id %>')">Browse Products</button>
                        </div>
                    </div>
                <% }); %>
            </div>
        <% } %>
    </div>

    <script>
        function browseVendor(vendorId) {
            window.location.href = '/user/vendor/' + vendorId + '/products';
        }

        function filterVendors() {
            const category = document.getElementById('categoryFilter').value;
            window.location.href = '/user/vendors' + (category ? '?category=' + encodeURIComponent(category) : '');
        }
    </script>
</body>
</html>
